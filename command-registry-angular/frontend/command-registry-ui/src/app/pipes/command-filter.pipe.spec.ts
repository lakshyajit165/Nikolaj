import { CommandFilterPipe } from './command-filter.pipe';

describe('CommandFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommandFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
